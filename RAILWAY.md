# วิธีการนำบอทขึ้น Railway

คู่มือนี้จะช่วยคุณนำบอท Discord ขึ้นไปรันบน Railway โดยไม่ต้องแก้โค้ดเพิ่ม

## 1. เตรียมโปรเจกต์
ตอนนี้โค้ดของคุณพร้อมสำหรับการ Deploy แล้ว ผมได้แก้โค้ดให้รองรับการอ่านรหัสผ่าน Firebase จาก Environment Variable เรียบร้อยแล้ว

## 2. ขั้นตอนการ Deploy บน Railway
1. เข้าไปที่ [Railway.app](https://railway.app/) แล้ว Login (แนะนำให้ใช้ GitHub Login)
2. กดปุ่ม `+ New Project` -> `Deploy from GitHub repo`
3. เลือก Repository ของบอทนี้ (Freyapa/BT-NESTLE)
4. รอสักครู่ Railway จะสร้างโปรเจกต์ให้ (แต่บอทจะยังรันไม่ได้ หรือ Error เพราะยังไม่ได้ใส่ตัวแปร)

## 3. การตั้งค่า Environment Variables (สำคัญมาก)
ไปที่แท็บ **Variables** ในโปรเจกต์ Railway ของคุณ แล้วเพิ่มค่าต่อไปนี้:

| ย่อชื่อตัวแปร (Variable Name) | ค่า (Value) | คำอธิบาย |
|---|---|---|
| `TOKEN` | `ใส่_Token_บอท_Discord_ที่นี่` | Token ของบอทจากหน้า Developer Portal |
| `FIREBASE_CREDENTIALS` | `ดูวิธีก๊อปปี้ด้านล่าง` | ข้อมูลจากไฟล์ serviceAccountKey.json |
| `JWT_SECRET` | `ตั้งรหัสผ่านอะไรก็ได้` | รหัสลับสำหรับสร้าง Token เว็บไซต์ |

### วิธีเอาค่า `FIREBASE_CREDENTIALS`
1. เปิดไฟล์ `config/serviceAccountKey.json` ในเครื่องคอมของคุณ
2. ก๊อปปี้ข้อความ **ทั้งหมด** ในไฟล์นั้น
3. นำไปวางในช่อง Value ของ `FIREBASE_CREDENTIALS` ใน Railway
   - **ระวัง:** ต้องก๊อปปี้มาให้ครบทุกตัวอักษร ตั้งแต่ `{` ตัวแรก จนถึง `}` ตัวสุดท้าย

## 4. เสร็จสิ้น
เมื่อใส่ตัวแปรครบแล้ว Railway จะทำการ Deploy ใหม่โดยอัตโนมัติ (หรือถ้าไม่ออโต้ ให้กด Redeploy)
รอจนขึ้นสีเขียว 🟢 ก็แปลว่าบอททำงานแล้วครับ!
